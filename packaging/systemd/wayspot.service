[Unit]
Description=Wayspot Notification DBus Interface
After=graphical-session.target
Wants=graphical-session.target

[Service]
Type=simple
# This service owns only the canonical notifications resident entry.
ExecStart=/usr/bin/wayspot notifications
Restart=on-failure
RestartSec=1

[Install]
WantedBy=default.target
