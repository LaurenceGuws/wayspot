providers -> allowed is 'mode'
search -> allowed is 'picker'
sdl_ -> loose the vendor prefix for domain nouns
get wrid of all 'generig', 'shared' type files/folders like 'commot', 'types'. We have allowed nouns and verbs, end of story

vendors are a means to an end, not domain models and not allowed nouns/verbs
our model goes like this
process
    notification
        banners(user interface)
    wallpaper
    sunglasses
    picker(user interface)
        mode(actions as list index)
            apps(open)
            notifications
                hisotry_list
                    TODO
                lifecycle(restart needs to move)
                    restart
            wallpaper
                lifecycle
                    restart
            sunglasses(first expansion of index candidate type)
                runtime config
                    red/blue filter
                    dimm
                    image

Round 2:

'1. approve_direct_hyprland_under_env
 SDL covers displays and Wayland surface/window handles, but not workspace/window inventory or monitor/workspace/window coupling. Approving this means direct Hyprland
 IPC is allowed only under src/env/hyprland.zig.'
    we quite frankly dont care aboutthis relatinship, we are keeping our model dumb and maluble.
    we need things like
    monitor(s)
        size and maybe scale depending if sdl can can handle it, sdl3 made this easier and it feels good currently 
        needs a minimum of current_acive list of workspace(s)-> workspaces may be visible to 1 or many monitors, this is not our policy to manage
        workspace
            same model but downward
            window
                size
                visible(might be floating or tiled, not our monkeys not our sircus)
                focused
this state is internal and shared between processes-commands-candidates etc etcas needed

  2 we can keep it under /env eentity candidates, but not feature complete in this sprint, prove the models are used correctly everywhere in the current code base
