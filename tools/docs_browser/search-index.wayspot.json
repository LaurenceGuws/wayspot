{
  "version": 1,
  "projectId": "wayspot",
  "generatedAt": "2026-04-02T16:53:53.202Z",
  "documents": [
    {
      "path": "README.md",
      "title": "wayspot",
      "headings": [
        "wayspot",
        "What It Is",
        "Quick Start",
        "Core Commands",
        "Routes",
        "Config",
        "Documentation",
        "Dev Loop",
        "Packaging"
      ],
      "text": "wayspot A Wayland shell daemon and launcher built in Zig. wayspot is not just a one shot app launcher anymore. The current shape is a long lived shell process with: a warm GTK launcher ( ui daemon, ui resident) a local control plane ( ctl ...) provider driven search and route handling Lua runtime config compositor aware WM integration through src/wm/ shell facing theme and wallpaper runtime paths What It Is Current emphasis: Wayland first launcher and shell surfaces deterministic daemon summon/hide/toggle behavior provider/ranking pipeline instead of ad hoc menu assembly Lua configured runtime behavior explicit WM integration boundaries Zig owned shell runtime paths for theme and wallpaper behavior Quick Start Build: Run the warm daemon: Summon from another shell: If you are working in this repo, use: Core Commands Launcher lifecycle: Control plane: Config/runtime: Theme/wallpaper runtime: Routes Current route prefixes: @ apps # windows ! workspaces recent dirs , themes % files & grep + packages ^ icons nerd icons : emoji $ notifications run = calculator ? web Config Runtime config lives at: /.config/wayspot/config.lua When missing, wayspot creates a default config automatically on startup paths that load runtime config. Theme state is now owned by Lua config, not shell env files. Documentation Use these as the real entrypoints: Docs Index Workflow Agent Handoff Architecture: App Shell Split Architecture: App Layering Architecture: Daemon Architecture Architecture: UI Architecture Architecture: Bootstrap Architecture: Config Architecture: Subsystem Inventory Architecture: Provider Inventory Architecture: Providers And Routes Architecture: DE Shell Vision Architecture: Control Plane Architecture: Engineering Local docs explorer: Project owned docs browser config lives under tools/docs browser/. Dev Loop Packaging packaging/arch/PKGBUILD packaging/systemd/wayspot.service packaging/desktop/wayspot.desktop",
      "sections": [
        {
          "id": "wayspot",
          "heading": "wayspot",
          "text": "A Wayland shell daemon and launcher built in Zig. wayspot is not just a one shot app launcher anymore. The current shape is a long lived shell process with: a warm GTK launcher ( ui daemon, ui resident) a local control plane ( ctl ...) provider driven search and route handling Lua runtime config compositor aware WM integration through src/wm/ shell facing theme and wallpaper runtime paths"
        },
        {
          "id": "what-it-is",
          "heading": "What It Is",
          "text": "Current emphasis: Wayland first launcher and shell surfaces deterministic daemon summon/hide/toggle behavior provider/ranking pipeline instead of ad hoc menu assembly Lua configured runtime behavior explicit WM integration boundaries Zig owned shell runtime paths for theme and wallpaper behavior"
        },
        {
          "id": "quick-start",
          "heading": "Quick Start",
          "text": "Build: Run the warm daemon: Summon from another shell: If you are working in this repo, use:"
        },
        {
          "id": "core-commands",
          "heading": "Core Commands",
          "text": "Launcher lifecycle: Control plane: Config/runtime: Theme/wallpaper runtime:"
        },
        {
          "id": "routes",
          "heading": "Routes",
          "text": "Current route prefixes: @ apps # windows ! workspaces recent dirs , themes % files & grep + packages ^ icons nerd icons : emoji $ notifications run = calculator ? web"
        },
        {
          "id": "config",
          "heading": "Config",
          "text": "Runtime config lives at: /.config/wayspot/config.lua When missing, wayspot creates a default config automatically on startup paths that load runtime config. Theme state is now owned by Lua config, not shell env files."
        },
        {
          "id": "documentation",
          "heading": "Documentation",
          "text": "Use these as the real entrypoints: Docs Index Workflow Agent Handoff Architecture: App Shell Split Architecture: App Layering Architecture: Daemon Architecture Architecture: UI Architecture Architecture: Bootstrap Architecture: Config Architecture: Subsystem Inventory Architecture: Provider Inventory Architecture: Providers And Routes Architecture: DE Shell Vision Architecture: Control Plane Architecture: Engineering Local docs explorer: Project owned docs browser config lives under tools/docs browser/."
        },
        {
          "id": "dev-loop",
          "heading": "Dev Loop",
          "text": ""
        },
        {
          "id": "packaging",
          "heading": "Packaging",
          "text": "packaging/arch/PKGBUILD packaging/systemd/wayspot.service packaging/desktop/wayspot.desktop"
        }
      ]
    },
    {
      "path": "docs/AGENT_HANDOFF.md",
      "title": "Agent Handoff",
      "headings": [
        "Agent Handoff",
        "Current Baseline",
        "Current Constraints",
        "Next"
      ],
      "text": "Agent Handoff Current Baseline GTK daemon summon/hide/toggle flow is the primary launcher lifecycle. Provider driven search is the current launcher model. Lua config is the active runtime state authority. WM integration currently targets Hyprland through src/wm/. Theme and wallpaper runtime paths now exist, but still need architectural cleanup. Current Constraints Keep changes aligned with docs/architecture/APP LAYERING.md. Keep shell/runtime behavior routed through app subsystems, not temporary shell glue. Keep route/provider behavior deterministic and narrow. Update the owning architecture doc when behavior changes materially. Next 1. Clean up theme/wallpaper integration against the documented subsystem boundaries. 2. Remove remaining hacked on behavior that bypasses config, action planning, or WM/runtime ownership. 3. Keep workstream notes and architecture docs aligned as each cleanup slice lands.",
      "sections": [
        {
          "id": "agent-handoff",
          "heading": "Agent Handoff",
          "text": ""
        },
        {
          "id": "current-baseline",
          "heading": "Current Baseline",
          "text": "GTK daemon summon/hide/toggle flow is the primary launcher lifecycle. Provider driven search is the current launcher model. Lua config is the active runtime state authority. WM integration currently targets Hyprland through src/wm/. Theme and wallpaper runtime paths now exist, but still need architectural cleanup."
        },
        {
          "id": "current-constraints",
          "heading": "Current Constraints",
          "text": "Keep changes aligned with docs/architecture/APP LAYERING.md. Keep shell/runtime behavior routed through app subsystems, not temporary shell glue. Keep route/provider behavior deterministic and narrow. Update the owning architecture doc when behavior changes materially."
        },
        {
          "id": "next",
          "heading": "Next",
          "text": "1. Clean up theme/wallpaper integration against the documented subsystem boundaries. 2. Remove remaining hacked on behavior that bypasses config, action planning, or WM/runtime ownership. 3. Keep workstream notes and architecture docs aligned as each cleanup slice lands."
        }
      ]
    },
    {
      "path": "docs/INDEX.md",
      "title": "Docs Index",
      "headings": [
        "Docs Index",
        "Start Here",
        "Core Architecture",
        "Subsystems",
        "Providers",
        "Current Workflow",
        "Ownership Rules"
      ],
      "text": "Docs Index Repo local docs map for contributors, operators, and agents. Use this file for repo workflow and architecture navigation, not as the public project landing page. Start Here AGENTS.md — repo local operating constraints. docs/AGENT HANDOFF.md — current focus, constraints, and entrypoints. docs/WORKFLOW.md — workflow and doc placement rules. README.md — top level overview and command entrypoints. Core Architecture docs/architecture/DE SHELL VISION.md — product direction and long lived daemon target. docs/architecture/APP SHELL SPLIT.md — app shell vs daemon vs UI split. docs/architecture/APP LAYERING.md — module boundaries and ownership split. docs/architecture/DAEMON ARCHITECTURE.md — resident runtime design and ownership. docs/architecture/UI ARCHITECTURE.md — UI runtime and GTK boundary design. docs/architecture/BOOTSTRAP.md — build, run, test, and local dev entrypoints. docs/architecture/CONFIG.md — Lua config ownership, consumers, and runtime behavior. docs/architecture/SUBSYSTEM INVENTORY.md — subsystem map with links to detailed docs. docs/architecture/PROVIDER INVENTORY.md — provider and route scoped search inventory. docs/architecture/PROVIDERS AND ROUTES.md — route/provider/action contract and isolation rules. docs/architecture/WA1 CONTROL PLANE SPEC.md — local control plane authority. docs/architecture/ENGINEERING.md — memory, ownership, threading, and review rules. Subsystems docs/subsystems/APP RUNTIME.md — app startup, mode selection, runtime assembly. docs/subsystems/CONFIG AND LUA.md — Lua config loading and config ownership. docs/subsystems/IPC CONTROL.md — local control plane and daemon command path. docs/subsystems/SEARCH SERVICE.md — provider collection, ranking, cache, and dynamic routes. docs/subsystems/SHELL MODULES.md — shell module lifecycle and event bus. docs/subsystems/UI RUNTIME.md — GTK/runtime UI implementation. docs/subsystems/NOTIFICATIONS.md — notification daemon/runtime and UI integration. docs/subsystems/WM INTEGRATION.md — WM backend contracts and Hyprland integration. docs/subsystems/THEME AND WALLPAPER RUNTIME.md — theme apply/state and wallpaper runtime. docs/subsystems/OBSERVABILITY.md — logging, metrics, telemetry, and health. Providers docs/providers/actions.md — static shell actions provider. docs/providers/apps.md — app launcher provider. docs/providers/dirs.md — directory/search jump provider. docs/providers/theme.md — theme route provider. docs/providers/windows.md — WM backed windows provider. docs/providers/workspaces.md — WM backed workspaces provider. docs/providers/web.md — route scoped web provider subsystem. docs/providers/calc.md — route scoped calculator subsystem. Current Workflow docs/WORKFLOW.md — work loop and documentation rules. docs/AGENT HANDOFF.md — current active focus. workstreams/ — active work queues and cleanup items. scripts/open docs browser.sh — local standalone docs explorer launcher for this repo. tools/docs browser/ — wayspot owned explorer config and docs index. Ownership Rules README.md is the public/project facing overview. docs/ is for workflow, navigation, and contributor/operator guidance. docs/architecture/ is current architecture authority. workstreams/ is the source of truth for active task tracking.",
      "sections": [
        {
          "id": "docs-index",
          "heading": "Docs Index",
          "text": "Repo local docs map for contributors, operators, and agents. Use this file for repo workflow and architecture navigation, not as the public project landing page."
        },
        {
          "id": "start-here",
          "heading": "Start Here",
          "text": "AGENTS.md — repo local operating constraints. docs/AGENT HANDOFF.md — current focus, constraints, and entrypoints. docs/WORKFLOW.md — workflow and doc placement rules. README.md — top level overview and command entrypoints."
        },
        {
          "id": "core-architecture",
          "heading": "Core Architecture",
          "text": "docs/architecture/DE SHELL VISION.md — product direction and long lived daemon target. docs/architecture/APP SHELL SPLIT.md — app shell vs daemon vs UI split. docs/architecture/APP LAYERING.md — module boundaries and ownership split. docs/architecture/DAEMON ARCHITECTURE.md — resident runtime design and ownership. docs/architecture/UI ARCHITECTURE.md — UI runtime and GTK boundary design. docs/architecture/BOOTSTRAP.md — build, run, test, and local dev entrypoints. docs/architecture/CONFIG.md — Lua config ownership, consumers, and runtime behavior. docs/architecture/SUBSYSTEM INVENTORY.md — subsystem map with links to detailed docs. docs/architecture/PROVIDER INVENTORY.md — provider and route scoped search inventory. docs/architecture/PROVIDERS AND ROUTES.md — route/provider/action contract and isolation rules. docs/architecture/WA1 CONTROL PLANE SPEC.md — local control plane authority. docs/architecture/ENGINEERING.md — memory, ownership, threading, and review rules."
        },
        {
          "id": "subsystems",
          "heading": "Subsystems",
          "text": "docs/subsystems/APP RUNTIME.md — app startup, mode selection, runtime assembly. docs/subsystems/CONFIG AND LUA.md — Lua config loading and config ownership. docs/subsystems/IPC CONTROL.md — local control plane and daemon command path. docs/subsystems/SEARCH SERVICE.md — provider collection, ranking, cache, and dynamic routes. docs/subsystems/SHELL MODULES.md — shell module lifecycle and event bus. docs/subsystems/UI RUNTIME.md — GTK/runtime UI implementation. docs/subsystems/NOTIFICATIONS.md — notification daemon/runtime and UI integration. docs/subsystems/WM INTEGRATION.md — WM backend contracts and Hyprland integration. docs/subsystems/THEME AND WALLPAPER RUNTIME.md — theme apply/state and wallpaper runtime. docs/subsystems/OBSERVABILITY.md — logging, metrics, telemetry, and health."
        },
        {
          "id": "providers",
          "heading": "Providers",
          "text": "docs/providers/actions.md — static shell actions provider. docs/providers/apps.md — app launcher provider. docs/providers/dirs.md — directory/search jump provider. docs/providers/theme.md — theme route provider. docs/providers/windows.md — WM backed windows provider. docs/providers/workspaces.md — WM backed workspaces provider. docs/providers/web.md — route scoped web provider subsystem. docs/providers/calc.md — route scoped calculator subsystem."
        },
        {
          "id": "current-workflow",
          "heading": "Current Workflow",
          "text": "docs/WORKFLOW.md — work loop and documentation rules. docs/AGENT HANDOFF.md — current active focus. workstreams/ — active work queues and cleanup items. scripts/open docs browser.sh — local standalone docs explorer launcher for this repo. tools/docs browser/ — wayspot owned explorer config and docs index."
        },
        {
          "id": "ownership-rules",
          "heading": "Ownership Rules",
          "text": "README.md is the public/project facing overview. docs/ is for workflow, navigation, and contributor/operator guidance. docs/architecture/ is current architecture authority. workstreams/ is the source of truth for active task tracking."
        }
      ]
    },
    {
      "path": "docs/WORKFLOW.md",
      "title": "Workflow + Docs Guide",
      "headings": [
        "Workflow + Docs Guide",
        "Audience",
        "Work Loop",
        "Workflow Rules",
        "Doc Placement",
        "Architecture Ownership"
      ],
      "text": "Workflow + Docs Guide This file explains how work and documentation are expected to flow in this repository. Audience README.md is public/project facing. This file is contributor/operator/agent facing. Work Loop 1. Read AGENTS.md. 2. Read docs/AGENT HANDOFF.md. 3. Read docs/INDEX.md. 4. Read the owning architecture doc for the slice. 5. Read the owning workstream item. 6. Implement one focused slice. 7. Update the owning docs in the same slice. 8. Validate locally. 9. Commit only at a healthy checkpoint. Workflow Rules Keep changes scoped to one real slice. Do not mix multiple invention lanes into one patch train. If behavior changes, update the owning doc in the same slice. If a doc contradicts code, fix the doc or remove the stale claim. Prefer one clear authority per topic. Keep docs/AGENT HANDOFF.md short and current. Doc Placement README.md Public overview, quick start, and major command entrypoints. scripts/open docs browser.sh Local launcher for the standalone docs explorer against this repo's docs surface. docs/ Workflow, navigation, and contributor/operator guidance. docs/architecture/ Current architecture authority and subsystem boundary rules. workstreams/ Active execution queues, cleanup lanes, and open follow up items. Architecture Ownership Launcher/daemon/product direction: docs/architecture/DE SHELL VISION.md Module and subsystem boundaries: docs/architecture/APP LAYERING.md Build/run/test and local bootstrap: docs/architecture/BOOTSTRAP.md Lua config ownership and runtime consumers: docs/architecture/CONFIG.md Providers, routes, and typed action integration: docs/architecture/PROVIDERS AND ROUTES.md Control socket and CLI daemon behavior: docs/architecture/WA1 CONTROL PLANE SPEC.md Memory/ownership/threading rules: docs/architecture/ENGINEERING.md",
      "sections": [
        {
          "id": "workflow-docs-guide",
          "heading": "Workflow + Docs Guide",
          "text": "This file explains how work and documentation are expected to flow in this repository."
        },
        {
          "id": "audience",
          "heading": "Audience",
          "text": "README.md is public/project facing. This file is contributor/operator/agent facing."
        },
        {
          "id": "work-loop",
          "heading": "Work Loop",
          "text": "1. Read AGENTS.md. 2. Read docs/AGENT HANDOFF.md. 3. Read docs/INDEX.md. 4. Read the owning architecture doc for the slice. 5. Read the owning workstream item. 6. Implement one focused slice. 7. Update the owning docs in the same slice. 8. Validate locally. 9. Commit only at a healthy checkpoint."
        },
        {
          "id": "workflow-rules",
          "heading": "Workflow Rules",
          "text": "Keep changes scoped to one real slice. Do not mix multiple invention lanes into one patch train. If behavior changes, update the owning doc in the same slice. If a doc contradicts code, fix the doc or remove the stale claim. Prefer one clear authority per topic. Keep docs/AGENT HANDOFF.md short and current."
        },
        {
          "id": "doc-placement",
          "heading": "Doc Placement",
          "text": "README.md Public overview, quick start, and major command entrypoints. scripts/open docs browser.sh Local launcher for the standalone docs explorer against this repo's docs surface. docs/ Workflow, navigation, and contributor/operator guidance. docs/architecture/ Current architecture authority and subsystem boundary rules. workstreams/ Active execution queues, cleanup lanes, and open follow up items."
        },
        {
          "id": "architecture-ownership",
          "heading": "Architecture Ownership",
          "text": "Launcher/daemon/product direction: docs/architecture/DE SHELL VISION.md Module and subsystem boundaries: docs/architecture/APP LAYERING.md Build/run/test and local bootstrap: docs/architecture/BOOTSTRAP.md Lua config ownership and runtime consumers: docs/architecture/CONFIG.md Providers, routes, and typed action integration: docs/architecture/PROVIDERS AND ROUTES.md Control socket and CLI daemon behavior: docs/architecture/WA1 CONTROL PLANE SPEC.md Memory/ownership/threading rules: docs/architecture/ENGINEERING.md"
        }
      ]
    },
    {
      "path": "docs/architecture/APP_LAYERING.md",
      "title": "App Layering",
      "headings": [
        "App Layering",
        "Layer Map",
        "Boundary Rules",
        "Provider Contract Notes"
      ],
      "text": "App Layering This file defines the current subsystem boundaries for wayspot. The goal is simple: make launcher, daemon, config, providers, WM integration, and shell runtime behavior composable instead of implicitly coupled. Layer Map 1. UI shell GTK/window lifecycle entry/input handling result rendering preview and popup UI 2. Search and providers query parsing route classification provider collection ranking action identifiers 3. Runtime services command planning/execution config loading async refresh cache ownership backend routed native execution for app owned runtime actions 4. Shell runtime tools theme apply wallpaper slideshow wallpaper sort/set 5. WM adapter output/workspace/window discovery focused output resolution compositor specific execution Boundary Rules UI shell: may consume parsed/ranked results may not own provider specific behavior may not embed shell/runtime hacks directly Providers: should emit typed candidates should not parse hidden UI state should not depend on widget behavior should only surface capabilities the runtime can actually fulfill Search/ranking: owns route filtering and route scoped candidate eligibility should not rely on provider title conventions alone when route isolation matters Runtime/config: owns user/runtime state should use Lua config for durable state should not use ad hoc env file state for first class features Shell runtime tools: own theme/wallpaper operational behavior should not be split across stale shell script sidecars unless explicitly documented as compatibility paths WM layer: compositor specific behavior belongs here or in runtime tools built on it UI and providers should not encode Hyprland specific assumptions directly window/workspace action execution should be routed through backend methods, not assembled as compositor command strings in UI/common Provider Contract Notes For new providers/routes: route parsing belongs in src/search/query.zig route filtering belongs in src/search/rank.zig provider collection belongs in src/providers/ .zig typed action execution belongs through src/ui/common/dispatch.zig If a route needs strict isolation, do not depend on broad candidate kinds alone. Use a dedicated action namespace or equivalent typed contract.",
      "sections": [
        {
          "id": "app-layering",
          "heading": "App Layering",
          "text": "This file defines the current subsystem boundaries for wayspot. The goal is simple: make launcher, daemon, config, providers, WM integration, and shell runtime behavior composable instead of implicitly coupled."
        },
        {
          "id": "layer-map",
          "heading": "Layer Map",
          "text": "1. UI shell GTK/window lifecycle entry/input handling result rendering preview and popup UI 2. Search and providers query parsing route classification provider collection ranking action identifiers 3. Runtime services command planning/execution config loading async refresh cache ownership backend routed native execution for app owned runtime actions 4. Shell runtime tools theme apply wallpaper slideshow wallpaper sort/set 5. WM adapter output/workspace/window discovery focused output resolution compositor specific execution"
        },
        {
          "id": "boundary-rules",
          "heading": "Boundary Rules",
          "text": "UI shell: may consume parsed/ranked results may not own provider specific behavior may not embed shell/runtime hacks directly Providers: should emit typed candidates should not parse hidden UI state should not depend on widget behavior should only surface capabilities the runtime can actually fulfill Search/ranking: owns route filtering and route scoped candidate eligibility should not rely on provider title conventions alone when route isolation matters Runtime/config: owns user/runtime state should use Lua config for durable state should not use ad hoc env file state for first class features Shell runtime tools: own theme/wallpaper operational behavior should not be split across stale shell script sidecars unless explicitly documented as compatibility paths WM layer: compositor specific behavior belongs here or in runtime tools built on it UI and providers should not encode Hyprland specific assumptions directly window/workspace action execution should be routed through backend methods, not assembled as compositor command strings in UI/common"
        },
        {
          "id": "provider-contract-notes",
          "heading": "Provider Contract Notes",
          "text": "For new providers/routes: route parsing belongs in src/search/query.zig route filtering belongs in src/search/rank.zig provider collection belongs in src/providers/ .zig typed action execution belongs through src/ui/common/dispatch.zig If a route needs strict isolation, do not depend on broad candidate kinds alone. Use a dedicated action namespace or equivalent typed contract."
        }
      ]
    },
    {
      "path": "docs/architecture/APP_SHELL_SPLIT.md",
      "title": "App Shell Split",
      "headings": [
        "App Shell Split",
        "What",
        "Why",
        "How",
        "App shell",
        "Daemon",
        "UI",
        "When",
        "Where",
        "Rules"
      ],
      "text": "Status: active Owner: shell Last Reviewed: 2026 03 17 Canonical: yes App Shell Split This document defines the top level split between the app shell, the daemon, and the UI process model. What wayspot is one executable with several operating modes: one shot CLI commands a long lived daemon like UI process ( ui daemon, ui resident) a summon first launcher path ( ui) runtime tools for theme, wallpaper, diagnostics, and control The important split is not \"CLI vs GUI\". It is: app shell and runtime orchestration in src/main.zig long lived daemon responsibilities in the resident UI path GTK UI composition and interaction code in src/ui/ Why This split exists so wayspot can behave like a shell component instead of a fresh process on every summon: startup cost is paid once control commands can target a warm process providers, caches, and WM state can stay hot shell modules can be added without collapsing everything into GTK glue How App shell The app shell lives primarily in src/main.zig. It is responsible for: parsing CLI mode deciding whether to take a one shot tool path or UI path loading config and runtime flags constructing the provider registry and SearchService starting the resident shell path when UI mode is selected Daemon The daemon is not a separate binary. It is the resident shell mode of the same program: wayspot ui daemon starts hidden and binds the local control socket wayspot ui resident starts visible and keeps the control/socket lifecycle wayspot ui first tries ctl summon; if that fails it starts a local UI UI The UI is an implementation selected by build options in src/ui/mod.zig: GTK shell when enable gtk=true stub shell for headless/test oriented builds The GTK shell composes launcher, notifications, control server integration, and module startup in src/ui/gtk shell.zig. When Use this split when deciding where new functionality belongs: if it is mode selection, runtime wiring, or service assembly: app shell if it is lifetime, control socket, summon/hide behavior, or warm state: daemon if it is widgets, interaction, rendering, focus, or GTK state: UI Where Primary ownership points: app shell: src/main.zig app state/bootstrap: src/app/bootstrap.zig UI abstraction: src/ui/mod.zig GTK runtime shell: src/ui/gtk shell.zig shell modules/event bus: src/shell/mod.zig local control plane: src/ipc/control.zig Rules Do not treat the daemon as a separate app with duplicated startup logic. Do not put command routing or runtime orchestration into GTK only files. Do not let UI widgets become the source of truth for daemon state. Do not introduce a second config or env state path when Lua config already owns the setting.",
      "sections": [
        {
          "id": "app-shell-split",
          "heading": "App Shell Split",
          "text": "This document defines the top level split between the app shell, the daemon, and the UI process model."
        },
        {
          "id": "what",
          "heading": "What",
          "text": "wayspot is one executable with several operating modes: one shot CLI commands a long lived daemon like UI process ( ui daemon, ui resident) a summon first launcher path ( ui) runtime tools for theme, wallpaper, diagnostics, and control The important split is not \"CLI vs GUI\". It is: app shell and runtime orchestration in src/main.zig long lived daemon responsibilities in the resident UI path GTK UI composition and interaction code in src/ui/"
        },
        {
          "id": "why",
          "heading": "Why",
          "text": "This split exists so wayspot can behave like a shell component instead of a fresh process on every summon: startup cost is paid once control commands can target a warm process providers, caches, and WM state can stay hot shell modules can be added without collapsing everything into GTK glue"
        },
        {
          "id": "how",
          "heading": "How",
          "text": ""
        },
        {
          "id": "app-shell",
          "heading": "App shell",
          "text": "The app shell lives primarily in src/main.zig. It is responsible for: parsing CLI mode deciding whether to take a one shot tool path or UI path loading config and runtime flags constructing the provider registry and SearchService starting the resident shell path when UI mode is selected"
        },
        {
          "id": "daemon",
          "heading": "Daemon",
          "text": "The daemon is not a separate binary. It is the resident shell mode of the same program: wayspot ui daemon starts hidden and binds the local control socket wayspot ui resident starts visible and keeps the control/socket lifecycle wayspot ui first tries ctl summon; if that fails it starts a local UI"
        },
        {
          "id": "ui",
          "heading": "UI",
          "text": "The UI is an implementation selected by build options in src/ui/mod.zig: GTK shell when enable gtk=true stub shell for headless/test oriented builds The GTK shell composes launcher, notifications, control server integration, and module startup in src/ui/gtk shell.zig."
        },
        {
          "id": "when",
          "heading": "When",
          "text": "Use this split when deciding where new functionality belongs: if it is mode selection, runtime wiring, or service assembly: app shell if it is lifetime, control socket, summon/hide behavior, or warm state: daemon if it is widgets, interaction, rendering, focus, or GTK state: UI"
        },
        {
          "id": "where",
          "heading": "Where",
          "text": "Primary ownership points: app shell: src/main.zig app state/bootstrap: src/app/bootstrap.zig UI abstraction: src/ui/mod.zig GTK runtime shell: src/ui/gtk shell.zig shell modules/event bus: src/shell/mod.zig local control plane: src/ipc/control.zig"
        },
        {
          "id": "rules",
          "heading": "Rules",
          "text": "Do not treat the daemon as a separate app with duplicated startup logic. Do not put command routing or runtime orchestration into GTK only files. Do not let UI widgets become the source of truth for daemon state. Do not introduce a second config or env state path when Lua config already owns the setting."
        }
      ]
    },
    {
      "path": "docs/architecture/BOOTSTRAP.md",
      "title": "Bootstrap",
      "headings": [
        "Bootstrap",
        "Build",
        "Run",
        "Test",
        "Useful Runtime Commands",
        "Theme + Wallpaper Runtime",
        "Notes"
      ],
      "text": "Bootstrap This doc owns practical build, run, test, and local dev entrypoints. Build Headless safe build: Run Direct run: Daemon path: Recommended local dev loop: re run.sh is the intended repo local rebuild/restart/summon path. Test Headless safe tests: Useful Runtime Commands Theme + Wallpaper Runtime Notes Live Hypr/Waybar integration currently assumes the user facing binary path is the installed wayspot binary, not only zig out/bin/wayspot. If behavior differs between repo changes and session behavior, verify which binary Hyprland is launching.",
      "sections": [
        {
          "id": "bootstrap",
          "heading": "Bootstrap",
          "text": "This doc owns practical build, run, test, and local dev entrypoints."
        },
        {
          "id": "build",
          "heading": "Build",
          "text": "Headless safe build:"
        },
        {
          "id": "run",
          "heading": "Run",
          "text": "Direct run: Daemon path: Recommended local dev loop: re run.sh is the intended repo local rebuild/restart/summon path."
        },
        {
          "id": "test",
          "heading": "Test",
          "text": "Headless safe tests:"
        },
        {
          "id": "useful-runtime-commands",
          "heading": "Useful Runtime Commands",
          "text": ""
        },
        {
          "id": "theme-wallpaper-runtime",
          "heading": "Theme + Wallpaper Runtime",
          "text": ""
        },
        {
          "id": "notes",
          "heading": "Notes",
          "text": "Live Hypr/Waybar integration currently assumes the user facing binary path is the installed wayspot binary, not only zig out/bin/wayspot. If behavior differs between repo changes and session behavior, verify which binary Hyprland is launching."
        }
      ]
    },
    {
      "path": "docs/architecture/CONFIG.md",
      "title": "Configuration",
      "headings": [
        "Configuration",
        "Source Of Truth",
        "Current Ownership Rules",
        "Current Theme State",
        "Runtime Consumers",
        "Save Behavior",
        "Validation Direction"
      ],
      "text": "Configuration Lua config is the runtime state authority for wayspot. This doc describes the current config ownership model and the runtime consumers that should read from it. Source Of Truth loader/parser: src/config/lua config.zig default template: src/config/default lua.zig config backed theme helper: src/tools/theme state.zig supported theme catalog: src/tools/theme catalog.zig Default path: /.config/wayspot/config.lua Override path: WAYSPOT CONFIG LUA Current Ownership Rules Durable runtime state belongs in Lua config. That includes: surface mode placement policy UI/runtime tool settings current theme This means first class features should not create parallel state stores in: shell env files ad hoc repo local state files UI only memory without a config backed source of truth Current Theme State Current theme is stored in Lua as: Theme switching should update Lua config first, then apply runtime side effects like Hyprland/Waybar theme pointers and reload behavior. Persisted theme values must come from the same supported theme authority used by runtime theme application. Invalid persisted only theme states should be impossible. The typed config model and Lua loader are expected to understand theme directly. Theme helpers should consume the config subsystem instead of parsing or rewriting config.lua ad hoc. Runtime Consumers Current config consumers include: launcher surface mode and placement runtime tool selection UI feature flags theme state Theme/wallpaper code should treat Lua config as the state authority and hyprpaper.conf as runtime execution input, not the primary truth source. Save Behavior Config writeback should happen through the config subsystem’s canonical renderer. That means: helpers should not do raw text surgery on config.lua config writes may normalize formatting the typed settings model remains the durable state authority Validation Direction Config loading should prefer: parse validate warn and default invalid fields where safe keep one clear source of truth per feature",
      "sections": [
        {
          "id": "configuration",
          "heading": "Configuration",
          "text": "Lua config is the runtime state authority for wayspot. This doc describes the current config ownership model and the runtime consumers that should read from it."
        },
        {
          "id": "source-of-truth",
          "heading": "Source Of Truth",
          "text": "loader/parser: src/config/lua config.zig default template: src/config/default lua.zig config backed theme helper: src/tools/theme state.zig supported theme catalog: src/tools/theme catalog.zig Default path: /.config/wayspot/config.lua Override path: WAYSPOT CONFIG LUA"
        },
        {
          "id": "current-ownership-rules",
          "heading": "Current Ownership Rules",
          "text": "Durable runtime state belongs in Lua config. That includes: surface mode placement policy UI/runtime tool settings current theme This means first class features should not create parallel state stores in: shell env files ad hoc repo local state files UI only memory without a config backed source of truth"
        },
        {
          "id": "current-theme-state",
          "heading": "Current Theme State",
          "text": "Current theme is stored in Lua as: Theme switching should update Lua config first, then apply runtime side effects like Hyprland/Waybar theme pointers and reload behavior. Persisted theme values must come from the same supported theme authority used by runtime theme application. Invalid persisted only theme states should be impossible. The typed config model and Lua loader are expected to understand theme directly. Theme helpers should consume the config subsystem instead of parsing or rewriting config.lua ad hoc."
        },
        {
          "id": "runtime-consumers",
          "heading": "Runtime Consumers",
          "text": "Current config consumers include: launcher surface mode and placement runtime tool selection UI feature flags theme state Theme/wallpaper code should treat Lua config as the state authority and hyprpaper.conf as runtime execution input, not the primary truth source."
        },
        {
          "id": "save-behavior",
          "heading": "Save Behavior",
          "text": "Config writeback should happen through the config subsystem’s canonical renderer. That means: helpers should not do raw text surgery on config.lua config writes may normalize formatting the typed settings model remains the durable state authority"
        },
        {
          "id": "validation-direction",
          "heading": "Validation Direction",
          "text": "Config loading should prefer: parse validate warn and default invalid fields where safe keep one clear source of truth per feature"
        }
      ]
    },
    {
      "path": "docs/architecture/DAEMON_ARCHITECTURE.md",
      "title": "Daemon Architecture",
      "headings": [
        "Daemon Architecture",
        "What",
        "Why",
        "How",
        "When",
        "Where",
        "Current Shape",
        "Rules"
      ],
      "text": "Status: active Owner: shell Last Reviewed: 2026 03 17 Canonical: yes Daemon Architecture This document describes the resident wayspot runtime as a daemon. What The daemon is the long lived wayspot process started through: wayspot ui daemon wayspot ui resident It is responsible for: owning the warm launcher/search runtime binding the local control socket keeping shell modules alive optionally bridging WM events into cache refresh keeping history, telemetry, and provider state in process answering bounded request/response control plane calls over a Unix stream socket owning resident slideshow lifecycle when wallpaper slideshow control is used Why The daemon exists because summon style shell tools should not pay full cold startup cost each time. A resident process allows: low latency summon/hide warm provider snapshots and caches module lifecycles with explicit health a single local control plane for in house shell integrations How Runtime assembly currently happens in src/main.zig: 1. parse CLI mode 2. load Lua config and runtime tool overrides 3. build Runtime 4. construct provider registry 5. construct SearchService 6. optionally start WM event bridge 7. start ui.Shell.run(...) The resident runtime currently owns: providers: ActionsProvider, AppsProvider, WindowsProvider, WorkspacesProvider, DirsProvider, ThemeProvider search/runtime service: SearchService telemetry: telemetry.zig module/event plumbing: src/shell/mod.zig control server: src/ipc/control.zig When The daemon exists only in resident UI modes. It should own a concern when that concern needs one or more of: warm state across summons local command/control surface long lived module health background refresh single writer runtime ownership resident child process lifecycle tied to shell behavior It should not own: one shot CLI tool behavior that can complete and exit cleanly pure rendering details provider specific ranking semantics Where Key files: runtime assembly: src/main.zig search/runtime core: src/app/search service.zig control socket: src/ipc/control.zig shell registry and event bus: src/shell/registry.zig, src/shell/event bus.zig GTK shell bootstrap: src/ui/gtk shell.zig Current Shape Today the daemon is still UI hosted rather than a standalone headless shelld. That is acceptable as the current implementation, but the boundary should still be treated as a shell runtime boundary, not as \"GTK owns everything\". Rules The control plane is daemon authority, not widget authority. Control plane transport must obey stream socket semantics, not single read or single write assumptions. Resident runtime state should be assembled once and reused. One shot tools should not silently create partial daemon state. New module work should integrate through shell/module and IPC boundaries, not direct GTK global state.",
      "sections": [
        {
          "id": "daemon-architecture",
          "heading": "Daemon Architecture",
          "text": "This document describes the resident wayspot runtime as a daemon."
        },
        {
          "id": "what",
          "heading": "What",
          "text": "The daemon is the long lived wayspot process started through: wayspot ui daemon wayspot ui resident It is responsible for: owning the warm launcher/search runtime binding the local control socket keeping shell modules alive optionally bridging WM events into cache refresh keeping history, telemetry, and provider state in process answering bounded request/response control plane calls over a Unix stream socket owning resident slideshow lifecycle when wallpaper slideshow control is used"
        },
        {
          "id": "why",
          "heading": "Why",
          "text": "The daemon exists because summon style shell tools should not pay full cold startup cost each time. A resident process allows: low latency summon/hide warm provider snapshots and caches module lifecycles with explicit health a single local control plane for in house shell integrations"
        },
        {
          "id": "how",
          "heading": "How",
          "text": "Runtime assembly currently happens in src/main.zig: 1. parse CLI mode 2. load Lua config and runtime tool overrides 3. build Runtime 4. construct provider registry 5. construct SearchService 6. optionally start WM event bridge 7. start ui.Shell.run(...) The resident runtime currently owns: providers: ActionsProvider, AppsProvider, WindowsProvider, WorkspacesProvider, DirsProvider, ThemeProvider search/runtime service: SearchService telemetry: telemetry.zig module/event plumbing: src/shell/mod.zig control server: src/ipc/control.zig"
        },
        {
          "id": "when",
          "heading": "When",
          "text": "The daemon exists only in resident UI modes. It should own a concern when that concern needs one or more of: warm state across summons local command/control surface long lived module health background refresh single writer runtime ownership resident child process lifecycle tied to shell behavior It should not own: one shot CLI tool behavior that can complete and exit cleanly pure rendering details provider specific ranking semantics"
        },
        {
          "id": "where",
          "heading": "Where",
          "text": "Key files: runtime assembly: src/main.zig search/runtime core: src/app/search service.zig control socket: src/ipc/control.zig shell registry and event bus: src/shell/registry.zig, src/shell/event bus.zig GTK shell bootstrap: src/ui/gtk shell.zig"
        },
        {
          "id": "current-shape",
          "heading": "Current Shape",
          "text": "Today the daemon is still UI hosted rather than a standalone headless shelld. That is acceptable as the current implementation, but the boundary should still be treated as a shell runtime boundary, not as \"GTK owns everything\"."
        },
        {
          "id": "rules",
          "heading": "Rules",
          "text": "The control plane is daemon authority, not widget authority. Control plane transport must obey stream socket semantics, not single read or single write assumptions. Resident runtime state should be assembled once and reused. One shot tools should not silently create partial daemon state. New module work should integrate through shell/module and IPC boundaries, not direct GTK global state."
        }
      ]
    },
    {
      "path": "docs/architecture/DE_SHELL_VISION.md",
      "title": "DE Shell Vision",
      "headings": [
        "DE Shell Vision",
        "North Star",
        "Current Baseline",
        "Architecture Direction",
        "Active Priorities",
        "Non-Goals"
      ],
      "text": "Status: active Owner: shell Last Reviewed: 2026 03 03 Canonical: yes DE Shell Vision This document defines the target shape of wayspot as a long lived shell daemon. North Star Build a modular daemon (shelld) that can run independently from the compositor and provide: launcher/summon UX notifications behavior session overlays (power, OSD, clipboard, quick actions) stable control interfaces for in house components Current Baseline wayspot currently has: resident/daemon GTK lifecycle ( ui resident, ui daemon) warm summon path local control plane IPC (ping, summon, hide, toggle, version, etc.) notifications popup stack with actions/history provider driven search pipeline background cache prewarm and async refresh strategy Architecture Direction 1. shelld process (long lived) owns lifecycle, IPC endpoints, module scheduler, telemetry 2. Modules launcher notifications osd session 3. Control plane local command surface (Unix socket) summon/hide/toggle, notify, dismiss, state query 4. UI surfaces launcher window notification popups/history OSD overlays 5. Placement stack placement engine (pure geometry policy) surface adapters (gtk toplevel, layer shell) wm adapter (hyprland now, compositor native later) Active Priorities 1. Keep daemon lifecycle and summon flow predictable. 2. Keep control plane behavior deterministic and documented. 3. Keep preview and popup UI abstractions generic. 4. Keep module boundaries clean as features expand. Non Goals writing a compositor in this repo building a full settings UI right now remote/network control transport",
      "sections": [
        {
          "id": "de-shell-vision",
          "heading": "DE Shell Vision",
          "text": "This document defines the target shape of wayspot as a long lived shell daemon."
        },
        {
          "id": "north-star",
          "heading": "North Star",
          "text": "Build a modular daemon (shelld) that can run independently from the compositor and provide: launcher/summon UX notifications behavior session overlays (power, OSD, clipboard, quick actions) stable control interfaces for in house components"
        },
        {
          "id": "current-baseline",
          "heading": "Current Baseline",
          "text": "wayspot currently has: resident/daemon GTK lifecycle ( ui resident, ui daemon) warm summon path local control plane IPC (ping, summon, hide, toggle, version, etc.) notifications popup stack with actions/history provider driven search pipeline background cache prewarm and async refresh strategy"
        },
        {
          "id": "architecture-direction",
          "heading": "Architecture Direction",
          "text": "1. shelld process (long lived) owns lifecycle, IPC endpoints, module scheduler, telemetry 2. Modules launcher notifications osd session 3. Control plane local command surface (Unix socket) summon/hide/toggle, notify, dismiss, state query 4. UI surfaces launcher window notification popups/history OSD overlays 5. Placement stack placement engine (pure geometry policy) surface adapters (gtk toplevel, layer shell) wm adapter (hyprland now, compositor native later)"
        },
        {
          "id": "active-priorities",
          "heading": "Active Priorities",
          "text": "1. Keep daemon lifecycle and summon flow predictable. 2. Keep control plane behavior deterministic and documented. 3. Keep preview and popup UI abstractions generic. 4. Keep module boundaries clean as features expand."
        },
        {
          "id": "non-goals",
          "heading": "Non-Goals",
          "text": "writing a compositor in this repo building a full settings UI right now remote/network control transport"
        }
      ]
    },
    {
      "path": "docs/architecture/ENGINEERING.md",
      "title": "Engineering",
      "headings": [
        "Engineering",
        "Design Rules",
        "Ownership Rules",
        "Threading Rules",
        "Feature Integration Rules",
        "Review Checklist"
      ],
      "text": "Engineering This file is the cross cutting engineering baseline for wayspot. Design Rules Build first class features through the app’s existing subsystems. Avoid one off integration paths that bypass routing, config, or execution layers. Prefer typed actions over opaque shell strings when the action belongs to the app. Keep provider output narrow and deterministic. Ownership Rules The code that allocates owns the free path unless explicitly documented otherwise. init/deinit pairs must free all owned buffers and generations. Provider owned strings must rotate cleanly across collects. Runtime helpers must document whether they mutate repo state, live config, or both. Threading Rules Do not hold locks across blocking subprocess or filesystem work. Long running runtime work should copy needed state and release locks first. Background daemon/service behavior must remain deterministic under repeated summon/reload paths. Feature Integration Rules For new shell facing features: 1. define the durable state owner 2. define the route/provider contract 3. define the execution/action contract 4. define the WM/runtime boundary 5. document the owning files If a feature cannot be described cleanly in those terms yet, it is probably still being hacked on rather than integrated properly. Review Checklist Does the feature use Lua config for durable state if it needs durable state? Does the provider emit typed candidates instead of UI coupled behavior? Does route isolation live in the route/rank layer instead of title matching? Does execution go through command planning instead of random shell glue? Does the feature expose only capabilities that the runtime can actually fulfill? Are repo docs and live behavior both updated?",
      "sections": [
        {
          "id": "engineering",
          "heading": "Engineering",
          "text": "This file is the cross cutting engineering baseline for wayspot."
        },
        {
          "id": "design-rules",
          "heading": "Design Rules",
          "text": "Build first class features through the app’s existing subsystems. Avoid one off integration paths that bypass routing, config, or execution layers. Prefer typed actions over opaque shell strings when the action belongs to the app. Keep provider output narrow and deterministic."
        },
        {
          "id": "ownership-rules",
          "heading": "Ownership Rules",
          "text": "The code that allocates owns the free path unless explicitly documented otherwise. init/deinit pairs must free all owned buffers and generations. Provider owned strings must rotate cleanly across collects. Runtime helpers must document whether they mutate repo state, live config, or both."
        },
        {
          "id": "threading-rules",
          "heading": "Threading Rules",
          "text": "Do not hold locks across blocking subprocess or filesystem work. Long running runtime work should copy needed state and release locks first. Background daemon/service behavior must remain deterministic under repeated summon/reload paths."
        },
        {
          "id": "feature-integration-rules",
          "heading": "Feature Integration Rules",
          "text": "For new shell facing features: 1. define the durable state owner 2. define the route/provider contract 3. define the execution/action contract 4. define the WM/runtime boundary 5. document the owning files If a feature cannot be described cleanly in those terms yet, it is probably still being hacked on rather than integrated properly."
        },
        {
          "id": "review-checklist",
          "heading": "Review Checklist",
          "text": "Does the feature use Lua config for durable state if it needs durable state? Does the provider emit typed candidates instead of UI coupled behavior? Does route isolation live in the route/rank layer instead of title matching? Does execution go through command planning instead of random shell glue? Does the feature expose only capabilities that the runtime can actually fulfill? Are repo docs and live behavior both updated?"
        }
      ]
    },
    {
      "path": "docs/architecture/PROVIDERS_AND_ROUTES.md",
      "title": "Providers And Routes",
      "headings": [
        "Providers And Routes",
        "Layer Responsibilities",
        "Query parsing",
        "Provider collection",
        "Ranking and route isolation",
        "Action planning and execution",
        "Recommended Pattern For New Routes",
        "Theme Route Example"
      ],
      "text": "Providers And Routes This file defines the current contract for adding search routes and providers to wayspot. It exists because route/provider bugs are easy to create when route parsing, provider collection, ranking, and execution planning are treated as the same problem. Layer Responsibilities Query parsing Owns: route prefix parsing raw term extraction no provider specific behavior Primary file: src/search/query.zig Provider collection Owns: emitting candidates for one subsystem owning provider local strings/generations reporting provider health Primary files: src/providers/ .zig Providers should not: parse hidden UI state depend on widget behavior assume route specific ranking behavior will save an overly broad candidate set Ranking and route isolation Owns: route to kind eligibility route specific score shaping rejecting candidates that do not belong to a route Primary file: src/search/rank.zig If a route needs strict isolation, score shaping alone is not enough. Use one of: a dedicated candidate kind a dedicated action namespace another explicit typed discriminator Action planning and execution Owns: mapping candidate actions into runnable commands distinguishing typed actions from provider owned execution specs telemetry labels and failure behavior Primary file: src/ui/common/dispatch.zig If the action belongs to the app, prefer a typed action id over a literal shell command payload. Runtime shell execution should be resolved from a provider owned action spec, not smuggled through candidate action text. Recommended Pattern For New Routes 1. Add the route prefix in src/search/query.zig. 2. Decide the action namespace or typed discriminator. 3. Implement the provider with narrow candidate output. 4. Implement route isolation in src/search/rank.zig. 5. Implement execution in src/ui/common/dispatch.zig. 6. Update help/default loadout/UI hints. 7. Add tests for: route parsing route isolation action planning Theme Route Example The theme route is a good example of the correct pattern now: route prefix: , provider emits typed actions: theme apply:<name ranker rejects non theme apply: rows for the theme route dispatch resolves theme apply: into wayspot apply theme <name This prevents generic action candidates from leaking into the theme route.",
      "sections": [
        {
          "id": "providers-and-routes",
          "heading": "Providers And Routes",
          "text": "This file defines the current contract for adding search routes and providers to wayspot. It exists because route/provider bugs are easy to create when route parsing, provider collection, ranking, and execution planning are treated as the same problem."
        },
        {
          "id": "layer-responsibilities",
          "heading": "Layer Responsibilities",
          "text": ""
        },
        {
          "id": "query-parsing",
          "heading": "Query parsing",
          "text": "Owns: route prefix parsing raw term extraction no provider specific behavior Primary file: src/search/query.zig"
        },
        {
          "id": "provider-collection",
          "heading": "Provider collection",
          "text": "Owns: emitting candidates for one subsystem owning provider local strings/generations reporting provider health Primary files: src/providers/ .zig Providers should not: parse hidden UI state depend on widget behavior assume route specific ranking behavior will save an overly broad candidate set"
        },
        {
          "id": "ranking-and-route-isolation",
          "heading": "Ranking and route isolation",
          "text": "Owns: route to kind eligibility route specific score shaping rejecting candidates that do not belong to a route Primary file: src/search/rank.zig If a route needs strict isolation, score shaping alone is not enough. Use one of: a dedicated candidate kind a dedicated action namespace another explicit typed discriminator"
        },
        {
          "id": "action-planning-and-execution",
          "heading": "Action planning and execution",
          "text": "Owns: mapping candidate actions into runnable commands distinguishing typed actions from provider owned execution specs telemetry labels and failure behavior Primary file: src/ui/common/dispatch.zig If the action belongs to the app, prefer a typed action id over a literal shell command payload. Runtime shell execution should be resolved from a provider owned action spec, not smuggled through candidate action text."
        },
        {
          "id": "recommended-pattern-for-new-routes",
          "heading": "Recommended Pattern For New Routes",
          "text": "1. Add the route prefix in src/search/query.zig. 2. Decide the action namespace or typed discriminator. 3. Implement the provider with narrow candidate output. 4. Implement route isolation in src/search/rank.zig. 5. Implement execution in src/ui/common/dispatch.zig. 6. Update help/default loadout/UI hints. 7. Add tests for: route parsing route isolation action planning"
        },
        {
          "id": "theme-route-example",
          "heading": "Theme Route Example",
          "text": "The theme route is a good example of the correct pattern now: route prefix: , provider emits typed actions: theme apply:<name ranker rejects non theme apply: rows for the theme route dispatch resolves theme apply: into wayspot apply theme <name This prevents generic action candidates from leaking into the theme route."
        }
      ]
    },
    {
      "path": "docs/architecture/UI_ARCHITECTURE.md",
      "title": "UI Architecture",
      "headings": [
        "UI Architecture",
        "What",
        "Why",
        "How",
        "When",
        "Where",
        "Rules"
      ],
      "text": "Status: active Owner: ui Last Reviewed: 2026 03 17 Canonical: yes UI Architecture This document describes the wayspot UI layer. What The UI layer renders and controls user facing shell surfaces: launcher/search window results list, preview, and interaction flow notification surfaces and popup handling shell actions tied to visibility, placement, and lifecycle The UI is an adapter over runtime services, not the source of application truth. Why The UI layer exists to keep: rendering and input handling separate from provider/runtime logic GTK specific code isolated from headless or future UI implementations summon/hide behavior consistent even as surfaces expand How UI selection happens in src/ui/mod.zig: GTK shell when compiled with GTK stub shell otherwise The GTK implementation is organized as: shell composition: src/ui/gtk shell.zig bootstrap/layout: src/ui/gtk/bootstrap.zig, src/ui/gtk/bootstrap layout.zig controller/search orchestration: src/ui/gtk/controller.zig, src/ui/gtk/shell controller.zig, src/ui/gtk/async search.zig lifecycle/control glue: src/ui/gtk/shell control.zig, src/ui/gtk/shell lifecycle.zig, src/ui/gtk/shell actions.zig rendering helpers: src/ui/gtk/render.zig, src/ui/gtk/results flow.zig, src/ui/gtk/preview.zig route/query UX: src/ui/gtk/query helpers.zig, src/ui/gtk/default loadout.zig, src/ui/gtk/help panel.zig Default loadout rendering is a UI concern, but default loadout selection policy is owned by SearchService. GTK should render rows returned by runtime, not assemble its own merged suggestion set. When UI files should own work when the behavior is about: rendering keybindings and pointer interaction selection/preview state layout and placement adapters shell lifecycle from the surface point of view They should not own: provider collection logic config authority command planning semantics WM backend parsing Where Primary folders: top level UI abstraction: src/ui/ GTK implementation: src/ui/gtk/ placement: src/ui/placement/ surface mode contracts: src/ui/surfaces/ headless support: src/ui/headless/ Rules GTK widgets should consume runtime outputs, not create hidden app state. GTK widgets should not own suggestion merging or history weighting policy. Route affordances belong in the query UX layer, not inside providers. UI defaults and help must reflect actual runtime behavior, not aspirations. Headless/stub paths should remain valid for diagnostics and test builds.",
      "sections": [
        {
          "id": "ui-architecture",
          "heading": "UI Architecture",
          "text": "This document describes the wayspot UI layer."
        },
        {
          "id": "what",
          "heading": "What",
          "text": "The UI layer renders and controls user facing shell surfaces: launcher/search window results list, preview, and interaction flow notification surfaces and popup handling shell actions tied to visibility, placement, and lifecycle The UI is an adapter over runtime services, not the source of application truth."
        },
        {
          "id": "why",
          "heading": "Why",
          "text": "The UI layer exists to keep: rendering and input handling separate from provider/runtime logic GTK specific code isolated from headless or future UI implementations summon/hide behavior consistent even as surfaces expand"
        },
        {
          "id": "how",
          "heading": "How",
          "text": "UI selection happens in src/ui/mod.zig: GTK shell when compiled with GTK stub shell otherwise The GTK implementation is organized as: shell composition: src/ui/gtk shell.zig bootstrap/layout: src/ui/gtk/bootstrap.zig, src/ui/gtk/bootstrap layout.zig controller/search orchestration: src/ui/gtk/controller.zig, src/ui/gtk/shell controller.zig, src/ui/gtk/async search.zig lifecycle/control glue: src/ui/gtk/shell control.zig, src/ui/gtk/shell lifecycle.zig, src/ui/gtk/shell actions.zig rendering helpers: src/ui/gtk/render.zig, src/ui/gtk/results flow.zig, src/ui/gtk/preview.zig route/query UX: src/ui/gtk/query helpers.zig, src/ui/gtk/default loadout.zig, src/ui/gtk/help panel.zig Default loadout rendering is a UI concern, but default loadout selection policy is owned by SearchService. GTK should render rows returned by runtime, not assemble its own merged suggestion set."
        },
        {
          "id": "when",
          "heading": "When",
          "text": "UI files should own work when the behavior is about: rendering keybindings and pointer interaction selection/preview state layout and placement adapters shell lifecycle from the surface point of view They should not own: provider collection logic config authority command planning semantics WM backend parsing"
        },
        {
          "id": "where",
          "heading": "Where",
          "text": "Primary folders: top level UI abstraction: src/ui/ GTK implementation: src/ui/gtk/ placement: src/ui/placement/ surface mode contracts: src/ui/surfaces/ headless support: src/ui/headless/"
        },
        {
          "id": "rules",
          "heading": "Rules",
          "text": "GTK widgets should consume runtime outputs, not create hidden app state. GTK widgets should not own suggestion merging or history weighting policy. Route affordances belong in the query UX layer, not inside providers. UI defaults and help must reflect actual runtime behavior, not aspirations. Headless/stub paths should remain valid for diagnostics and test builds."
        }
      ]
    },
    {
      "path": "workstreams/code_review_todo.yaml",
      "title": "code_review_todo.yaml",
      "headings": [],
      "text": "items: id: REVIEW 01 title: Cap greedy shell capture in grep route execution status: in progress owner: shell files: src/app/search service/dynamic routes.zig src/app/search service/query context.zig done: Replaced unbounded grep capture with a bounded cap. Added explicit StreamTooLong log path when grep output exceeds cap. Added bounded shell capture regression tests (StdoutStreamTooLong + exit handling). Added UI/headless truncation hint when route output exceeds cap. Removed swallowed icon route shell capture failures by propagating errors. todo: Add tests for oversized grep output behavior and OOM guard semantics. Add integration assertion that huge result sets do not OOM the daemon. notes: Highest priority memory risk; isolate and close quickly. id: REVIEW 02 title: Constrain async result cache growth and polling window status: in progress owner: shell files: src/ui/gtk/results flow.zig src/ui/gtk/async state.zig src/ui/gtk/async search.zig done: Bounded async payload rows before marshalling to UI. Added bounded cache window in async state with truncation logging. Added bounded scroll driven window growth in results flow. Hardened cache cap math against overflow and added cap clamp/saturation tests. todo: Introduce bounded result cache with explicit max size. Ensure poll windows are bounded by UI demand. Make cache replacement policy explicit and tested under rapid scroll. Keep list model updates scroll safe. notes: Affects UI memory footprint and responsiveness during repeated polling. id: REVIEW 03 title: Move heavy route work off GTK main thread status: in progress owner: shell files: src/ui/gtk/results flow.zig src/ui/gtk shell.zig src/app/search service/query engine.zig done: Confirmed hot paths: non route/default queries remain sync in results flow, while route heavy prefixes were still dispatched to sync searchQuery. Expanded async route eligibility to include %, &, $, = prefixes in UI route detection so heavy dynamic route collectors stay off the GTK main thread. Added tests covering async route detection edge cases for route prefixes. todo: If profiler shows still blocking path for other query routes/providers, expand async offloading scope and measure UI thread latency. Add integration test ensuring stale async payload generation drops results when query changes mid flight. notes: UI jank risk is highest during large searches. id: REVIEW 04 title: Remove stale ToolState snapshoting of executable/env state status: in progress owner: shell files: src/app/search service/dynamic routes.zig done: Added freshness window for dynamic tool availability and hidden flag checks. Added explicit cache invalidation API for ToolState. Added cache miss logging when tool/hidden state changes. todo: Validate tools state on each command execution or add explicit cache invalidation. Avoid stale tool/env assumptions across daemon lifetime. Add telemetry when tool availability changes. notes: Prevents intermittent route misbehavior and false negatives. id: REVIEW 05 title: Fix help row signal/user data lifecycle status: completed owner: shell files: src/ui/gtk/bootstrap.zig done: Added GClosureNotify cleanup for per row help state in GTK signal connections. Added toggle state cleanup callback that frees shared help panel state on widget destruction. Limited per row state allocation to interactive rows only. todo: Validate no leaks under repetitive popup reopen. notes: Memory hygiene for repeated info/help interactions. id: REVIEW 06 title: Remove IPC single read truncation risk status: completed owner: shell files: src/ipc/control.zig src/main.zig done: Removed fixed size response read by switching client side to chunked reads. Reworked server response serialization to avoid fixed response size buffer truncation. Added endpoint aware server write response logs for oversized payload diagnostics. Added response decode diagnostics and read telemetry (bytes/reads/offset/line/column) on client. todo: None. notes: Prevents silent truncation/failure on larger control responses. id: REVIEW 07 title: Improve diagnostics surface quality status: completed owner: shell files: src/ipc/control.zig src/main.zig src/app/search service/ done: Added control command response helper path with route specific logging and elapsed timing. Added structured control command logs (endpoint/route/elapsed ns/exit code) for query and non query paths. Added CLI action context logs on control command rejection/failure in main. todo: None. notes: Makes future debugging faster for daemon and protocol issues. id: REVIEW 08 title: Re audit results streaming invariants after recent changes status: in progress owner: shell files: src/ui/gtk/results flow.zig src/ui/gtk/render.zig done: Added result window size into render hash so cached async rows re render when pagination grows. Prevented async ready callback from always forcing first row selection (which reset scroll during pagination). todo: Verify list model and rendering contracts no longer rely on full result assumptions. Preserve scroll position under item churn. Keep popup/info widgets outside main blocking path when possible. notes: Aligns with current architecture after prior UI changes.",
      "sections": []
    }
  ]
}
